name: CI - Loader Faster Unit Tests

on:
  push:
    paths:
      - 'services/loader_faster/**'
  pull_request:
    paths:
      - 'services/loader_faster/**'
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: services/loader_faster

    steps:
      - uses: actions/checkout@v3

      - uses: actions/setup-python@v4
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: |
          pip install uv
          uv sync --dev  # installs runtime deps + pytest + tools from [tool.uv].dev-dependencies

      - name: Run tests
        env:
          PYTHONPATH: ${{ github.workspace }}/services/loader_faster/src/api-service
        run: |
          uv run pytest tests/unit/ -v --cov=api

      - name: System tests info
        run: |
          echo "✓ System tests available locally:"
          echo "  docker-compose -f docker-compose.test.yml up --build --abort-on-container-exit"
